#[derive(Clone, Copy, Debug)]
pub struct HDRPixel {
    pub r: f32,
    pub g: f32,
    pub b: f32,
}

#[derive(Clone, Copy, Debug)]
pub struct LDRPixel {
    pub r: f32,
    pub g: f32,
    pub b: f32,
}
